# Инструкция по запуску мини-приложения

## Быстрый старт

1. **Установите зависимости:**
   ```bash
   npm install
   ```

2. **Запустите API сервер (в отдельном терминале):**
   ```bash
   npm run dev:api
   ```
   Сервер будет доступен на `http://localhost:3000`

3. **Запустите чат-бота:**
   ```bash
   npm run dev
   ```

4. **Откройте мини-приложение:**
   - В браузере: `http://localhost:3000`
   - Или через кнопку "📱 В приложение" в чат-боте

## Использование

### В чат-боте:
1. Откройте бота в MAX
2. Нажмите кнопку "📱 В приложение" в главном меню
3. Откроется мини-приложение в браузере

### В мини-приложении:
- **Расписание** - просмотр расписания на сегодня, завтра или неделю
- **Преподаватели** - поиск и просмотр расписания преподавателей
- **Мероприятия** - календарь мероприятий на ближайшие 7 дней
- **Дедлайны** - управление дедлайнами (добавление, удаление)
- **Настройки** - выбор группы, подгруппы, настройка уведомлений

## Настройка URL мини-приложения

Для продакшена установите переменную окружения:

```bash
export MINI_APP_URL=https://your-domain.com
```

Или в файле `.env`:
```
MINI_APP_URL=https://your-domain.com
```

## Структура файлов

- `mini-app/index.html` - HTML структура
- `mini-app/css/style.css` - Стили
- `mini-app/js/app.js` - JavaScript логика
- `src/api-server.ts` - API сервер

## API эндпоинты

Все API эндпоинты доступны по адресу `/api/*`:
- `/api/user/:userId` - данные пользователя
- `/api/schedule/:userId` - расписание
- `/api/events` - мероприятия
- `/api/deadlines/:userId` - дедлайны
- `/api/teachers` - преподаватели
- и другие...

Подробнее см. в `src/api-server.ts`

