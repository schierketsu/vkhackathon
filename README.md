# Кампус - Чат-бот и мини-приложение для MAX

Чат-бот и мини-приложение для студентов с расписанием, мероприятиями, дедлайнами и информацией о преподавателях.

## ✨ Особенности

- 📅 **Расписание занятий** - просмотр расписания на сегодня, завтра или неделю
- 👨‍🏫 **Преподаватели** - поиск преподавателей и их расписание
- 🎉 **Мероприятия** - календарь мероприятий с возможностью подписки
- ⏰ **Дедлайны** - управление дедлайнами с уведомлениями
- 📱 **Мини-приложение** - веб-приложение с интерфейсом в стиле MAX UI

## 🚀 Быстрый старт

### Требования

- Node.js 18+ 
- npm или yarn
- Токен бота от MAX (получите через [Master Bot](https://max.ru/masterbot))

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd vkhackathon
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` в корне проекта:
```env
BOT_TOKEN=your_bot_token_here
MINI_APP_URL=https://your-domain.com
```

### Запуск

1. **Запуск бота:**
```bash
npm run dev
```

2. **Запуск API сервера для мини-приложения:**
```bash
npm run dev:api
```

## 📱 Мини-приложение

Мини-приложение создано с использованием:
- **MAX Bridge** - для взаимодействия с API MAX и получения данных пользователя
- **MAX UI Design System** - стилизация под интерфейс MAX
- Стандартные веб-технологии (HTML, CSS, JavaScript)

### Размещение мини-приложения

⚠️ **Важно:** MAX API требует публичный HTTPS URL. `localhost` не поддерживается.

#### Для локальной разработки используйте туннель:

1. **ngrok** (рекомендуется):
```bash
# Установите ngrok: https://ngrok.com/download
# Запустите API сервер
npm run dev:api

# В другом терминале запустите ngrok
ngrok http 3000

# Скопируйте HTTPS URL (например: https://abc123.ngrok.io)
# Установите переменную окружения:
export MINI_APP_URL=https://abc123.ngrok.io
```

2. **localtunnel**:
```bash
npm install -g localtunnel
lt --port 3000
```

3. **serveo.net**:
```bash
ssh -R 80:localhost:3000 serveo.net
```

#### Для продакшена:

Загрузите файлы мини-приложения на хостинг с поддержкой HTTPS:
- Vercel
- GitHub Pages
- Netlify
- Любой другой статический хостинг

Установите `MINI_APP_URL` на ваш публичный домен.

### Подключение мини-приложения к боту

1. Откройте [личный кабинет MAX для партнёров](https://dev.max.ru)
2. Выберите ваш чат-бот
3. Перейдите в раздел "Чат-бот и мини-приложение"
4. Вставьте URL вашего мини-приложения
5. Сохраните изменения

После этого в чате с ботом появится кнопка для быстрого запуска мини-приложения.

### MAX Bridge API

Мини-приложение использует MAX Bridge для:
- Получения данных пользователя через `window.MAXBridge.getUser()`
- Получения стартовых параметров через `window.MAXBridge.getStartParam()`
- Отправки данных обратно в бот через `WebApp.sendData()`
- Использования кнопки "Назад" MAX
- Применения темы MAX (светлая/темная)

### Структура мини-приложения

```
mini-app/
├── index.html      # Главный HTML файл с MAX Bridge SDK
├── css/
│   └── style.css   # Стили в стиле MAX UI Design System
└── js/
    └── app.js      # Логика приложения с интеграцией MAX Bridge
```

## 🔧 API Сервер

API сервер предоставляет REST API для мини-приложения:

- `GET /api/user/:userId` - получение данных пользователя
- `PUT /api/user/:userId/group` - обновление группы
- `PUT /api/user/:userId/subgroup` - обновление подгруппы
- `PUT /api/user/:userId/setting` - обновление настройки
- `GET /api/schedule/:userId?period=today` - получение расписания
- `GET /api/teachers` - список всех преподавателей
- `GET /api/teachers/search?q=query` - поиск преподавателей
- `GET /api/events` - список мероприятий
- `GET /api/deadlines/:userId` - список дедлайнов
- `POST /api/deadlines` - добавление дедлайна
- `DELETE /api/deadlines/:id` - удаление дедлайна
- `GET /api/groups` - список всех групп
- `GET /api/subgroups/:groupName` - список подгрупп для группы

## 📚 Команды бота

- `/start` - начало работы с ботом
- `/help` - справка по командам
- `/сегодня` - расписание на сегодня
- `/завтра` - расписание на завтра
- `/неделя` - расписание на неделю
- `/группа` - выбор группы
- `/подгруппа` - выбор подгруппы
- `/поиск <имя>` - поиск преподавателя
- `/мероприятия` - ближайшие мероприятия
- `/подписка` - подписка на уведомления
- `/дедлайны` - список дедлайнов
- `/новыйдедлайн <название> <дата>` - добавить дедлайн
- `/уведомления` - настройки уведомлений

## 🗄️ База данных

Приложение использует SQLite3 для хранения данных:
- Пользователи и их настройки
- Дедлайны
- Избранные преподаватели

База данных автоматически создается при первом запуске в файле `data/campus.db`.

## 📝 Структура проекта

```
vkhackathon/
├── src/
│   ├── bot.ts              # Главный файл бота
│   ├── api-server.ts       # API сервер для мини-приложения
│   ├── handlers/           # Обработчики команд бота
│   └── utils/              # Утилиты и вспомогательные функции
├── mini-app/               # Мини-приложение
│   ├── index.html
│   ├── css/
│   └── js/
├── data/                   # Данные (БД, JSON файлы)
└── dist/                   # Скомпилированный код
```

## 🔐 Безопасность

- Все URL мини-приложения должны использовать HTTPS
- Токен бота храните в переменных окружения, не коммитьте в репозиторий
- Проверяйте данные от мини-приложения перед обработкой

## 📖 Документация

- [MAX Bot API Documentation](https://dev.max.ru)
- [MAX Mini Apps Documentation](https://dev.max.ru/docs/webapps/introduction)
- [MAX UI Design System](https://dev.max.ru/ui)

## 🤝 Вклад

Приветствуются любые улучшения! Создавайте issues и pull requests.

## 📄 Лицензия

MIT
