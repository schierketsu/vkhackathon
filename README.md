# Кампус - Чат-бот для MAX

Чат-бот для студентов с расписанием, мероприятиями, дедлайнами и информацией о преподавателях.

## ✨ Особенности

- 📅 **Расписание занятий** - просмотр расписания на сегодня, завтра или неделю
- 👨‍🏫 **Преподаватели** - поиск преподавателей и их расписание
- 🎉 **Мероприятия** - календарь мероприятий с возможностью подписки
- ⏰ **Дедлайны** - управление дедлайнами с уведомлениями
- 📱 **Мини-приложение** - веб-интерфейс для всех функций (работает на хостинге)
- 🤖 **Чат-бот** - работает только локально, использует локальный API сервер

## 🚀 Быстрый старт

### Требования

- Node.js 18+ 
- npm или yarn
- Токен бота от MAX (получите через [Master Bot](https://max.ru/masterbot))

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd vkhackathon
```

2. Установите зависимости:
```bash
npm install
cd chatbot && npm install
cd ../miniapp && npm install
cd ../miniapp/api-server && npm install
```

3. Создайте файл `.env` в корне проекта (опционально):
```env
BOT_TOKEN=your_bot_token_here
```

## 🖥️ Локальный запуск

### Порядок запуска компонентов

Для локальной работы необходимо запустить компоненты в следующем порядке:

1. **API сервер** (общий для чат-бота и мини-приложения)
2. **Чат-бот** (использует локальный API)
3. **Мини-приложение** (опционально, для тестирования веб-интерфейса)

### Шаг 1: Запуск API сервера

API сервер используется и чат-ботом, и мини-приложением. Запустите его первым:

**В первом терминале:**
```bash
npm run api:dev
```

Или из папки `miniapp/api-server/`:
```bash
cd miniapp/api-server
npm run dev
```

API сервер запустится на порту **3002** и будет доступен по адресу `http://localhost:3002/api`.

**Проверка:** Откройте в браузере `http://localhost:3002/api/health` - должно вернуться `{"status":"ok",...}`

### Шаг 2: Запуск чат-бота

**Важно:** Чат-бот работает **только локально** и использует **локальный API сервер** на `http://localhost:3002/api`.

**Во втором терминале:**
```bash
npm run dev:chatbot
```

Или из папки `chatbot/`:
```bash
cd chatbot
npm run dev
```

**Примечание:** 
- Убедитесь, что API сервер запущен перед запуском чат-бота
- Чат-бот использует локальную базу данных `data/campus.db`
- Чат-бот не работает без запущенного локального API сервера

### Шаг 3: Запуск мини-приложения (опционально)

Мини-приложение можно запустить для тестирования веб-интерфейса локально:

**В третьем терминале:**
```bash
cd
npm run dev
```

Мини-приложение будет доступно по адресу `http://localhost:3000`.

**Примечание:** 
- Для работы мини-приложения необходим запущенный API сервер на порту 3002
- В режиме разработки мини-приложение использует локальный API (`http://localhost:3002/api`)
- В продакшене (на хостинге) мини-приложение использует API на `https://maxhackathon.ru/api`

## 📚 Команды бота

- `/start` - начало работы с ботом
- `/help` - справка по командам
- `/сегодня` - расписание на сегодня
- `/завтра` - расписание на завтра
- `/неделя` - расписание на неделю
- `/группа` - выбор группы
- `/подгруппа` - выбор подгруппы
- `/поиск <имя>` - поиск преподавателя
- `/мероприятия` - ближайшие мероприятия
- `/подписка` - подписка на уведомления
- `/дедлайны` - список дедлайнов
- `/новыйдедлайн <название> <дата>` - добавить дедлайн
- `/уведомления` - настройки уведомлений

## 🗄️ База данных

Приложение использует SQLite3 для хранения данных:
- Пользователи и их настройки
- Дедлайны
- Избранные преподаватели
- Заявки на практику
- Отзывы о компаниях

База данных автоматически создается при первом запуске в файле `data/campus.db`.

**Важно:** 
- Чат-бот и API сервер используют **одну и ту же базу данных** `data/campus.db`
- При локальной работе оба компонента работают с локальной БД
- На хостинге API сервер использует БД на сервере, а чат-бот (локально) - свою локальную БД

## 📝 Структура проекта

```
vkhackathon/
├── chatbot/              # Чат-бот
│   ├── src/
│   │   ├── bot.ts        # Главный файл бота
│   │   ├── handlers/     # Обработчики команд бота
│   │   └── utils/        # Утилиты и вспомогательные функции
│   ├── package.json
│   └── tsconfig.json
├── miniapp/              # Мини-приложение
│   ├── api-server/       # API сервер (бэкенд)
│   │   ├── server.ts
│   │   ├── shared-utils.ts
│   │   └── package.json
│   ├── src/              # Код мини-приложения (фронтенд)
│   │   ├── api/
│   │   ├── components/
│   │   ├── pages/
│   │   └── ...
│   ├── package.json
│   └── vite.config.ts
├── data/                 # Данные (БД, JSON файлы)
│   ├── campus.db
│   ├── events.json
│   ├── timetable.json
│   └── ...
├── deploy/               # Конфигурация для деплоя
│   ├── ecosystem.config.js
│   ├── nginx.conf
│   └── .htaccess
└── package.json          # Корневой package.json
```

## 🔧 Доступные команды

**Чат-бот:**
- `npm run dev:chatbot` - запуск в режиме разработки
- `npm run build:chatbot` - сборка
- `npm run start:chatbot` - запуск собранной версии

**API сервер:**
- `npm run api:dev` - запуск в режиме разработки
- `npm run build:api` - сборка
- `npm run api:server` - запуск собранной версии

**Мини-приложение:**
- `cd miniapp && npm run dev` - запуск в режиме разработки
- `cd miniapp && npm run build` - сборка

**Деплой:**
- `npm run deploy:prepare` - собрать все для деплоя

## 🔐 Безопасность

- Токен бота храните в переменных окружения, не коммитьте в репозиторий
- API токены храните в `.env` файлах

## 📖 Документация

- [MAX Bot API Documentation](https://dev.max.ru)
- [Инструкция по деплою](DEPLOY.md)

## 🤝 Вклад

Приветствуются любые улучшения! Создавайте issues и pull requests.

## 📄 Лицензия

MIT
