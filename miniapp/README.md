# Кампус - Мини-приложение MAX

Мини-приложение для MAX, дублирующее функционал чат-бота Кампус.

## ✨ Функционал

- 📅 **Расписание занятий** - просмотр расписания на сегодня, завтра или неделю
- 👨‍🏫 **Преподаватели** - поиск преподавателей и их расписание
- 🎉 **Мероприятия** - календарь мероприятий с возможностью подписки
- ⏰ **Дедлайны** - управление дедлайнами с уведомлениями
- ⚙️ **Настройки** - выбор группы, подгруппы, управление уведомлениями

## 🚀 Установка и запуск

### Требования

- Node.js 18+
- npm или yarn

### Установка зависимостей

```bash
cd miniapp
npm install
```

### Запуск

1. **Запустите бота (который включает API сервер)** (в корне проекта):
```bash
cd ..
npm run dev
```

API сервер автоматически запустится на порту 3001 вместе с ботом.

2. **В другом терминале запустите мини-приложение** (в папке `miniapp`):
```bash
cd miniapp
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

**Примечание:** Теперь API сервер запускается вместе с ботом, поэтому не нужно запускать `npm run api` отдельно.

## 📁 Структура проекта

```
miniapp/
├── api/              # API сервер
│   ├── server.ts     # Express сервер
│   └── shared-utils.ts  # Общие утилиты
├── src/
│   ├── api/          # API клиент
│   ├── components/   # React компоненты
│   ├── pages/        # Страницы приложения
│   ├── App.tsx       # Главный компонент
│   └── main.tsx      # Точка входа
├── package.json
└── vite.config.ts
```

## 🔧 Настройка

### Переменные окружения

Создайте файл `.env` в папке `miniapp`:

```env
VITE_API_URL=http://localhost:3001/api
```

### База данных

Мини-приложение использует ту же базу данных, что и бот (`data/campus.db`). База данных создается автоматически при первом запуске API сервера.

## 🎨 Использование MAX UI

Приложение использует компоненты из библиотеки `@maxhub/max-ui`:
- `Panel` - контейнер приложения
- `Container` - контейнер контента
- `Grid` - сетка для расположения элементов
- `CellSimple`, `CellAction` - ячейки списка
- `Button` - кнопки
- `Typography` - типографика
- `Input`, `SearchInput` - поля ввода
- `Switch` - переключатели
- И другие компоненты

## 📝 Примечания

- Мини-приложение использует те же данные, что и бот (файлы JSON и база данных)
- Для полноценной работы необходимо реализовать функции получения расписания и списка преподавателей из `src/utils`
- В текущей версии некоторые функции упрощены (помечены TODO в коде)

## 🔗 Интеграция с MAX Bridge

Для интеграции с MAX Bridge в продакшене:
1. Получите `userId` из Bridge вместо localStorage
2. Настройте авторизацию через Bridge
3. Используйте Bridge API для получения данных пользователя

## 📄 Лицензия

MIT

