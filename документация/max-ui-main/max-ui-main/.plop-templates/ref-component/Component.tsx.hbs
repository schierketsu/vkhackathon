import { clsx } from 'clsx';
import { type ComponentProps, forwardRef } from 'react';

import styles from './{{properCase componentName}}.module.scss';

export interface {{properCase componentName}}Props extends ComponentProps<'div'> {}

export const {{properCase componentName}} = forwardRef<HTMLDivElement, {{properCase componentName}}Props>((props, forwardedRef) => {
  const {
    className,
    ...rest
  } = props;

  return (
    <div ref={forwardedRef} className={clsx(styles.{{properCase componentName}}, className)} {...rest} />
  );
});

{{properCase componentName}}.displayName = '{{properCase componentName}}';
