# 📦 Инструкция по деплою мини-приложения на maxhackathon.ru

## 📋 Содержание

1. [Подготовка к деплою](#подготовка-к-деплою)
2. [Сборка проекта](#сборка-проекта)
3. [Загрузка файлов на хостинг](#загрузка-файлов-на-хостинг)
4. [Настройка API сервера](#настройка-api-сервера)
5. [Настройка веб-сервера](#настройка-веб-сервера)
6. [Проверка работы](#проверка-работы)
7. [Обновление приложения](#обновление-приложения)
8. [Решение проблем](#решение-проблем)

---

## 🚀 Подготовка к деплою

### Шаг 1: Установка зависимостей

Откройте терминал в корне проекта и выполните:

```bash
# Установите зависимости для корневого проекта
npm install

# Установите зависимости для API сервера
cd miniapp/api-server
npm install

# Вернитесь в корень и установите зависимости для мини-приложения
cd ../..
cd miniapp
npm install
cd ..
```

### Шаг 2: Проверка структуры проекта

Убедитесь, что у вас есть следующие папки:
- `miniapp/` - мини-приложение (фронтенд)
- `miniapp/api-server/` - API сервер (бэкенд)
- `data/` - данные (база данных и JSON файлы)
- `deploy/` - конфигурационные файлы для деплоя

---

## 🔨 Сборка проекта

### Шаг 1: Сборка всех компонентов

В корне проекта выполните одну команду:

```bash
npm run deploy:prepare
```

Эта команда:
1. Соберет API сервер в `miniapp/api-server/dist/`
2. Соберет мини-приложение в `miniapp/dist/`

**Важно:** После сборки проверьте, что папки `dist/` созданы:
- `miniapp/dist/` - должна содержать `index.html` и папку `assets/`
- `miniapp/api-server/dist/` - должна содержать скомпилированные `.js` файлы

---

## 📤 Загрузка файлов на хостинг

### Шаг 1: Подготовка файлов для загрузки

Перед загрузкой подготовьте следующие файлы:

#### Для мини-приложения (фронтенд):
- Вся папка `miniapp/dist/` → загрузить в `public_html/`
- Файл `deploy/.htaccess` → загрузить в `public_html/` (если используете Apache)

#### Для API сервера (бэкенд):
- Вся папка `miniapp/api-server/dist/` → загрузить в `api-server/dist/` на хостинге
- Файл `miniapp/api-server/package.json` → загрузить в `api-server/` на хостинге
- Файл `miniapp/api-server/tsconfig.json` → загрузить в `api-server/` на хостинге (опционально)

#### Для данных:
- Вся папка `data/` → загрузить в корень домена (на том же уровне, что и `public_html/`)

### Шаг 2: Загрузка через ispmanager

#### 2.1. Загрузка мини-приложения

1. Войдите в **ispmanager**
2. Откройте **Файловый менеджер** для домена `maxhackathon.ru`
3. Перейдите в папку `public_html/`
4. **Удалите** все существующие файлы (если они есть)
5. Загрузите **все содержимое** папки `miniapp/dist/`:
   - Выберите все файлы из `miniapp/dist/` (включая `index.html` и папку `assets/`)
   - Загрузите их в `public_html/`
6. Загрузите файл `deploy/.htaccess` в `public_html/`

**Результат:** В `public_html/` должны быть:
```
public_html/
├── index.html
├── assets/
│   ├── index-xxxxx.js
│   ├── index-xxxxx.css
│   └── ...
└── .htaccess
```

#### 2.2. Загрузка API сервера

1. В **Файловом менеджере** создайте папку `api-server/` на уровне `public_html/` (в корне домена)
2. Загрузите в `api-server/`:
   - Папку `miniapp/api-server/dist/` → загрузить как `api-server/dist/`
   - Файл `miniapp/api-server/package.json` → загрузить как `api-server/package.json`

**Результат:** Структура должна быть:
```
/var/www/maxhackathon.ru/
├── public_html/          (мини-приложение)
├── api-server/          (API сервер)
│   ├── dist/
│   └── package.json
└── data/                (данные)
```

#### 2.3. Загрузка данных

1. В **Файловом менеджере** создайте папку `data/` на уровне `public_html/`
2. Загрузите все файлы из локальной папки `data/`:
   - `campus.db`
   - `events.json`
   - `timetable.json`
   - `practice-companies.json`
   - `forparser.json`
   - `config.json` (если есть)

---

## ⚙️ Настройка API сервера

### Шаг 1: Создание файла .env

1. В **Файловом менеджере** создайте файл `.env` в папке `api-server/`
2. Добавьте в него следующее содержимое:

```env
NODE_ENV=production
PORT=3002
DB_PATH=/var/www/maxhackathon.ru/data/campus.db
QWEN_API_TOKEN=ваш_токен_здесь
```

**Важно:** 
- Замените `/var/www/maxhackathon.ru/` на реальный путь к вашему домену на хостинге
- Обычно путь можно узнать в ispmanager в разделе "Домены" → "Путь к файлам"
- Если не знаете токен QWEN_API_TOKEN, оставьте значение по умолчанию из кода

### Шаг 2: Установка зависимостей на сервере

Через **SSH** (если есть доступ) или через **Терминал** в ispmanager:

```bash
# Перейдите в папку API сервера
cd /var/www/maxhackathon.ru/api-server

# Установите зависимости (только production)
npm install --production
```

**Если SSH недоступен:**
- В ispmanager найдите раздел "Node.js" или "Node.js менеджер"
- Создайте новое приложение и укажите путь к `api-server/`
- ispmanager автоматически установит зависимости

### Шаг 3: Запуск API сервера

#### Вариант 1: Через PM2 (рекомендуется)

PM2 - это менеджер процессов для Node.js, который автоматически перезапускает сервер при сбоях.

```bash
# Установите PM2 глобально (если еще не установлен)
npm install -g pm2

# Перейдите в корень проекта
cd /var/www/maxhackathon.ru

# Загрузите файл ecosystem.config.js в корень проекта (если еще не загружен)
# Затем запустите:
pm2 start deploy/ecosystem.config.js

# Сохраните конфигурацию PM2
pm2 save

# Настройте автозапуск при перезагрузке сервера
pm2 startup
```

**Проверка работы:**
```bash
# Посмотрите список процессов
pm2 list

# Посмотрите логи
pm2 logs campus-api
```

#### Вариант 2: Через ispmanager Node.js менеджер

1. В ispmanager найдите раздел **"Node.js"** или **"Node.js менеджер"**
2. Нажмите **"Создать приложение"**
3. Заполните:
   - **Имя приложения:** `campus-api`
   - **Путь к приложению:** `/var/www/maxhackathon.ru/api-server`
   - **Файл запуска:** `dist/server.js`
   - **Порт:** `3002`
   - **Переменные окружения:** добавьте переменные из `.env` файла
4. Нажмите **"Создать"** и **"Запустить"**

#### Вариант 3: Через systemd (для продвинутых)

Если у вас есть root доступ, можно настроить systemd сервис (инструкции по запросу).

---

## 🌐 Настройка веб-сервера

### Для Apache (через .htaccess)

Если вы уже загрузили файл `deploy/.htaccess` в `public_html/`, настройка должна работать автоматически.

**Проверка:**
- Убедитесь, что в ispmanager включен модуль `mod_rewrite` для Apache
- Файл `.htaccess` должен быть в `public_html/`

### Для Nginx

Если ваш хостинг использует Nginx:

1. В ispmanager найдите раздел **"Nginx"** или **"Веб-сервер"**
2. Откройте конфигурацию для домена `maxhackathon.ru`
3. Добавьте конфигурацию из файла `deploy/nginx.conf`

**Основные настройки:**
- Проксирование `/api` на `http://localhost:3002`
- SPA routing (все запросы на `index.html`)
- Кеширование статических файлов

**Если нет доступа к конфигурации Nginx:**
- Обратитесь в поддержку хостинга с просьбой настроить проксирование `/api` на `localhost:3002`

---

## ✅ Проверка работы

### Шаг 1: Проверка мини-приложения

1. Откройте в браузере: `https://maxhackathon.ru`
2. Должна открыться главная страница мини-приложения
3. Если видите ошибку 404 или пустую страницу:
   - Проверьте, что файл `index.html` загружен в `public_html/`
   - Проверьте права доступа к файлам (должны быть 644 для файлов, 755 для папок)

### Шаг 2: Проверка API

1. Откройте в браузере: `https://maxhackathon.ru/api/health`
2. Должен вернуться JSON: `{"status":"ok","timestamp":"..."}`
3. Если видите ошибку:
   - Проверьте, что API сервер запущен (`pm2 list` или через ispmanager)
   - Проверьте логи API сервера
   - Проверьте, что порт 3002 не занят другим приложением

### Шаг 3: Проверка работы мини-приложения с API

1. В мини-приложении попробуйте:
   - Загрузить расписание
   - Посмотреть мероприятия
   - Открыть страницу преподавателей
2. Если данные не загружаются:
   - Откройте консоль браузера (F12) и посмотрите ошибки
   - Проверьте, что API отвечает (см. Шаг 2)
   - Проверьте CORS настройки в API сервере

---

## 🔄 Обновление приложения

Когда нужно обновить приложение:

### Шаг 1: Сборка новой версии

На локальном компьютере:

```bash
# Соберите проект заново
npm run deploy:prepare
```

### Шаг 2: Загрузка обновлений

1. **Обновите мини-приложение:**
   - Загрузите новые файлы из `miniapp/dist/` в `public_html/` (замените старые)

2. **Обновите API сервер:**
   - Загрузите новые файлы из `miniapp/api-server/dist/` в `api-server/dist/` (замените старые)
   - Если изменился `package.json`, загрузите его и выполните `npm install --production` на сервере

3. **Обновите данные (если нужно):**
   - Загрузите обновленные файлы из `data/` на сервер

### Шаг 3: Перезапуск API сервера

```bash
# Если используете PM2
pm2 restart campus-api

# Или через ispmanager Node.js менеджер
# Остановите и запустите приложение заново
```

---

## 🔧 Решение проблем

### Проблема: API не отвечает

**Решение:**
1. Проверьте, что API сервер запущен:
   ```bash
   pm2 list
   # или
   ps aux | grep node
   ```

2. Проверьте логи:
   ```bash
   pm2 logs campus-api
   # или
   tail -f /var/www/maxhackathon.ru/api-server/logs/api-error.log
   ```

3. Проверьте, что порт 3002 не занят:
   ```bash
   netstat -tulpn | grep 3002
   ```

4. Проверьте файл `.env` - правильный ли путь к БД и порт

### Проблема: CORS ошибки в браузере

**Симптомы:** В консоли браузера видите ошибку типа "CORS policy" или "Access-Control-Allow-Origin"

**Решение:**
1. Убедитесь, что домен `maxhackathon.ru` добавлен в `allowedOrigins` в файле `api-server/server.ts`
2. Пересоберите API сервер: `npm run build:api`
3. Загрузите обновленный `api-server/dist/` на сервер
4. Перезапустите API сервер

### Проблема: База данных не найдена

**Симптомы:** API возвращает ошибку "Database not found" или "Cannot open database"

**Решение:**
1. Проверьте путь к БД в файле `.env`:
   ```env
   DB_PATH=/var/www/maxhackathon.ru/data/campus.db
   ```
2. Убедитесь, что файл `data/campus.db` существует на сервере
3. Проверьте права доступа к файлу БД (должны быть 644 или 666)
4. Проверьте, что путь в `.env` абсолютный (начинается с `/`)

### Проблема: Статические файлы не загружаются

**Симптомы:** Страница открывается, но нет стилей, картинок или скриптов

**Решение:**
1. Проверьте, что папка `assets/` загружена в `public_html/`
2. Проверьте права доступа:
   - Файлы: 644
   - Папки: 755
3. Проверьте, что файл `.htaccess` загружен в `public_html/`
4. Проверьте, что в ispmanager включен модуль `mod_rewrite` для Apache

### Проблема: Страница открывается, но показывает ошибку 404 при переходе по ссылкам

**Симптомы:** Главная страница работает, но при переходе на другие страницы (например, `/schedule`) показывается 404

**Решение:**
1. Это проблема SPA routing - убедитесь, что файл `.htaccess` загружен в `public_html/`
2. Для Nginx: убедитесь, что настроено правило `try_files $uri $uri/ /index.html;`
3. Проверьте, что в ispmanager включен `mod_rewrite` для Apache

### Проблема: PM2 не запускается

**Решение:**
1. Убедитесь, что PM2 установлен: `npm list -g pm2`
2. Проверьте файл `deploy/ecosystem.config.js` - правильные ли пути
3. Попробуйте запустить вручную:
   ```bash
   cd /var/www/maxhackathon.ru/api-server
   node dist/server.js
   ```
   Если работает вручную, проблема в конфигурации PM2

### Проблема: Не могу найти путь к домену

**Решение:**
1. В ispmanager откройте раздел **"Домены"**
2. Найдите домен `maxhackathon.ru`
3. Посмотрите колонку **"Путь"** или **"Документ рут"** - это и есть путь к домену
4. Обычно это `/var/www/maxhackathon.ru/` или `/home/username/domains/maxhackathon.ru/public_html/`

---

## 📁 Итоговая структура на хостинге

После успешного деплоя структура должна выглядеть так:

```
/var/www/maxhackathon.ru/          (или ваш путь к домену)
│
├── public_html/                   ← Мини-приложение (фронтенд)
│   ├── index.html
│   ├── assets/
│   │   ├── index-xxxxx.js
│   │   ├── index-xxxxx.css
│   │   └── ...
│   └── .htaccess
│
├── api-server/                    ← API сервер (бэкенд)
│   ├── dist/
│   │   └── server.js
│   ├── package.json
│   ├── node_modules/
│   └── .env
│
├── data/                          ← Данные
│   ├── campus.db
│   ├── events.json
│   ├── timetable.json
│   ├── practice-companies.json
│   ├── forparser.json
│   └── config.json
│
└── deploy/                        ← Конфигурация (опционально)
    ├── nginx.conf
    ├── .htaccess
    └── ecosystem.config.js
```

---

## 💡 Полезные команды

### Проверка статуса API сервера
```bash
pm2 list
pm2 logs campus-api
pm2 restart campus-api
pm2 stop campus-api
pm2 start campus-api
```

### Просмотр логов
```bash
# Логи PM2
pm2 logs campus-api --lines 100

# Логи напрямую (если настроены)
tail -f /var/www/maxhackathon.ru/api-server/logs/api-out.log
tail -f /var/www/maxhackathon.ru/api-server/logs/api-error.log
```

### Проверка портов
```bash
netstat -tulpn | grep 3002
lsof -i :3002
```

---

## 📞 Нужна помощь?

Если что-то не работает:

1. Проверьте логи API сервера
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все файлы загружены правильно
4. Проверьте права доступа к файлам
5. Обратитесь в поддержку хостинга, если проблема с настройкой веб-сервера

---

**Удачи с деплоем! 🚀**
